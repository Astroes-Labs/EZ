<div class="text-center mb-16">
    <h2 class="text-4xl sm:text-5xl lg:text-6xl font-bold tracking-tight">
        {!! $title !!}
    </h2>
    @if($subtitle)
        <p class="mt-6 text-xl text-gray-400 max-w-3xl mx-auto leading-relaxed">{{ $subtitle }}</p>
    @endif
</div>